# Use TypeScript Node Runtime

Bitcoin Bouncer's MVP runtime is a TypeScript/Node.js service that hosts the HTTP API, Decision Queue, Bitcoin Core RPC integration, Live Agent calls, and SQLite state store. This keeps the Polar sandbox easy to script while fitting the agent/tool-call and web-service shape of the project.
